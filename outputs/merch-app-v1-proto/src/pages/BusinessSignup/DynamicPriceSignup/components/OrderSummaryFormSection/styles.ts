import styled from 'styled-components';
import { colors } from '@pages/BusinessSignup/styles';

export const DynamicPricingExplainer = styled.p`
  font-family: 'Open Sans';
  font-size: 15px;
  line-height: 28px;
  color: ${colors.neutral[35]};
  margin-bottom: -5px;
`;
